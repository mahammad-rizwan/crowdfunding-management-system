// const express = require('express');
// const router = express.Router();
// const { galleryController, upload } = require('../controllers/galleryController');

// router.post('/upload', upload.array('photos', 10), galleryController.uploadPhotos);


// router.get('/:title', galleryController.getPhotosByCampaign);

// module.exports = router;
